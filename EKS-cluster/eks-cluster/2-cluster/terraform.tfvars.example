# Copy to terraform.tfvars and fill in real values.
# Find account IDs: AWS Console → Organizations → AWS accounts

account_id     = "000000000000"  # dev account ID
admin_role_arn = "arn:aws:iam::000000000000:role/aws-reserved/sso.amazonaws.com/AWSReservedSSO_AdministratorAccess_XXXXXXXXXXXXXXXX"
